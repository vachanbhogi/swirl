{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum core permissions for the local Swirl main window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
