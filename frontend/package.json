{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "cd .. && jac start --dev main.jac --port 14217",
    "build": "cd .. && jac build main.jac --client web",
    "test": "cd .. && jac clean --data --force && jac test backend/engine_tests.jac -v",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}
