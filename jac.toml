[project]
name = "swirl"
version = "0.1.0"
description = "Command-line Jac project"
entry-point = "main.jac"
kind = "cli"
jac-version = "==0.34.7"

[dependencies]

[dev-dependencies]
watchdog = ">=3.0.0"
